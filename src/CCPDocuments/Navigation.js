import React from 'react';
import PropTypes from 'prop-types';

const Navigatin = ({ components }) => {
  return (
    <div>

    </div>
  );
};

Navigatin.propTypes = {
  // components: PropTypes.array.isRequired
};

export default Navigatin;
